{{- if not .Values.existingConfigMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tilegroxy.fullname" . }}-config
  labels:
    {{- include "tilegroxy.labels" . | nindent 4 }}
data:
  {{ base .Values.configPath }}: |
    {{- include "tilegroxy.config" . | nindent 4 }}
{{- end }}
