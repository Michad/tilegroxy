{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tilegroxy.secretName" . }}
  labels:
    {{- include "tilegroxy.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end }}
